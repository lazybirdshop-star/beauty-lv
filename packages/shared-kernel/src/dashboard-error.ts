/**
 * Почему действие в кабинете не прошло, — значением, о котором договорились
 * обе стороны.
 *
 * Причина та же, что у `AUTH_ERROR_CODES` и `BOOKING_ERROR_CODES`, но повод
 * другой и он важнее: те экраны работают до входа, а этот — после, и язык
 * мастера продукту уже известен. Тем заметнее было расхождение. Сообщения
 * `NestJS` написаны по-русски, кабинет говорит на трёх языках, и два экрана
 * печатали серверную фразу дословно: английская форма записи отвечала
 * «Это время уже прошло».
 *
 * По HTTP-статусу эти отказы не различить — «окно только что заняли»,
 * «время уже прошло» и «для выбранных услуг не хватает времени подряд»
 * приходят одним 409, а сказать их надо разными словами: в первом случае
 * помогает обновление страницы, во втором — другое окно, в третьем — другой
 * набор услуг.
 *
 * Фраза в `message` остаётся: это то, что увидит разработчик в логах и в
 * ответе API. Мастер её не видит никогда — кабинет показывает свой перевод по
 * коду, а незнакомый код сводит к общей фразе (`describeApiError` на вебе).
 */
export const DASHBOARD_ERROR_CODES = {
  /* ── Окна ────────────────────────────────────────────────────────────── */
  /** Время уже прошло: опубликовать, перенести или записать в него нельзя. */
  slotInPast: 'slot_in_past',
  /** Окно на это время у мастера уже открыто. */
  slotDuplicate: 'slot_duplicate',
  /** Окна с таким id нет — скорее всего, его только что сняли. */
  slotNotFound: 'slot_not_found',
  /** Окно занято записью: снять или перенести его можно только после отмены. */
  slotBooked: 'slot_booked',
  /** Гонка: пока страница лежала открытой, окно заняли. */
  slotJustTaken: 'slot_just_taken',
  /** Массовая публикация: весь выбранный период уже в прошлом. */
  slotsAllPast: 'slots_all_past',
  /**
   * Окно попадает внутрь уже идущего визита.
   *
   * Не `slot_booked` и не `slot_duplicate`: окна на это время нет вовсе — есть
   * визит, который через него идёт. Мастер опубликовала бы время, которое уже
   * продано, и продукт продал бы его второй раз.
   */
  slotInsideBooking: 'slot_inside_booking',

  /* ── Записи ──────────────────────────────────────────────────────────── */
  /** Между окнами не хватает непрерывного времени под выбранные услуги. */
  notEnoughTime: 'not_enough_time',
  /** В корзине нет ни одной услуги. */
  noServices: 'no_services',
  /** Записи с таким id нет. */
  bookingNotFound: 'booking_not_found',
  /**
   * Запись уже завершена или отменена — править её нельзя.
   *
   * Не «не найдена»: запись есть, и мастер её видит. У завершённой цены в
   * позициях уже посчитаны как доход, у отменённой окна отданы и могли быть
   * проданы — обе заводятся заново, а не правятся.
   */
  bookingNotEditable: 'booking_not_editable',

  /* ── Каталог и клиенты ───────────────────────────────────────────────── */
  serviceNotFound: 'service_not_found',
  categoryNotFound: 'category_not_found',
  clientNotFound: 'client_not_found',
  /** Клиент с этим номером уже есть в адресной книге. */
  clientPhoneTaken: 'client_phone_taken',

  /* ── Заявки на регистрацию ───────────────────────────────────────────── */
  /**
   * Одобрить нельзя: адрес заявки принадлежит аккаунту мастера или
   * администратора. Клиента платформа повышает сама, этих — нет: решение,
   * что делать с двумя мастерами на один адрес, принимает человек.
   */
  registrationEmailIsMaster: 'registration_email_is_master',
  /** Аккаунт с этим адресом заблокирован — повышать нечего, пока он такой. */
  registrationAccountBlocked: 'registration_account_blocked',
  /** Телефон из заявки уже за другим аккаунтом, а он уникален. */
  registrationPhoneTaken: 'registration_phone_taken',
  /** Гонка: пока заявку одобряли, адрес занял кто-то ещё. */
  registrationEmailTaken: 'registration_email_taken',
  /**
   * Решение по заявке уже принято — своё или чужое.
   *
   * Отдельно от общей фразы: «не получилось, проверьте связь» отправило бы
   * администратора чинить интернет там, где чинить нечего — очередь просто
   * ушла вперёд, и достаточно её обновить.
   */
  registrationRequestDecided: 'registration_request_decided',

  /* ── Пользователи платформы ──────────────────────────────────────────── */
  /**
   * Действие направлено на себя — сменить себе роль или заблокировать себя
   * нельзя.
   *
   * Обе кнопки стояли в списке пользователей на собственной строке
   * администратора, и оба запроса отвечали `200`: единственный администратор
   * платформы становился клиентом, после чего заявки некому одобрять, роль
   * некому вернуть, а восстановление — прямой `UPDATE` в базе.
   */
  cannotTargetSelf: 'cannot_target_self',
  /**
   * Последнего администратора платформы разжаловать нельзя.
   *
   * Отдельно от `cannot_target_self`: двое администраторов, разжаловавшие друг
   * друга одновременно, попадают сюда, а не туда, — и второй запрос обязан
   * отказать, иначе платформа остаётся без единого администратора.
   */
  lastAdmin: 'last_admin',

  /* ── Команда ─────────────────────────────────────────────────────────── */
  /** Тариф не разрешает больше участников — SALON.md §8.3. */
  teamMemberLimitReached: 'team_member_limit_reached',
  /** Этот человек уже состоит в организации. */
  teamAlreadyMember: 'team_already_member',
  /** Живое приглашение на этот адрес уже выпущено — его надо отозвать или дождаться. */
  teamInviteAlreadySent: 'team_invite_already_sent',
  /**
   * Последнего владельца снимать нельзя.
   *
   * Отдельно от `cannot_target_self`: владельцев может быть несколько, и
   * второй, снимающий первого одновременно с ним, попадает сюда.
   */
  teamLastOwner: 'team_last_owner',
  /**
   * Роль владельца здесь не меняется.
   *
   * Передача владения — не смена роли: за ней стоят подписка, юридическое
   * лицо и ответственность за данные салона. Это отдельный разговор и
   * отдельное действие, а не выпадающий список в списке участников.
   */
  teamOwnerRoleLocked: 'team_owner_role_locked',
  /** Ссылка приглашения неизвестна, отозвана, протухла или уже сработала. */
  teamInviteInvalid: 'team_invite_invalid',
  /** Ссылка выписана на другой адрес почты, чем у вошедшего. */
  teamInviteEmailMismatch: 'team_invite_email_mismatch',
  /**
   * Этот аккаунт в салон не принять: администратор платформы или
   * заблокированный. Решение принимает человек, а не переход по ссылке.
   */
  teamAccountNotJoinable: 'team_account_not_joinable',
} as const;

export type DashboardErrorCode = (typeof DASHBOARD_ERROR_CODES)[keyof typeof DASHBOARD_ERROR_CODES];

export function isDashboardErrorCode(value: unknown): value is DashboardErrorCode {
  return (
    typeof value === 'string' && (Object.values(DASHBOARD_ERROR_CODES) as string[]).includes(value)
  );
}
