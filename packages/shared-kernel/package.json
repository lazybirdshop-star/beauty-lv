{
  "name": "@amolie/shared-kernel",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-module domain primitives shared by apps/web and apps/api (see ARCHITECTURE.md §5).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint --config ../../eslint.config.mjs src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
