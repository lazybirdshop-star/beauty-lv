---
target: дашборд
total_score: 29
max_score: 40
na_heuristics:
p0_count: 1
p1_count: 4
target_identity: 'file:/Users/main/Documents/Projects/Beaty/apps/web/src/app/[slug]/dashboard'
timestamp: 2026-09-16T11-13-00Z
slug: apps-web-src-app-slug-dashboard
---

Method: dual-agent (A: design review · B: detector + browser), выводы сверены по коду и скриншотам

## Design Health Score — 29/40 (23 → 24 → 27 → 28 → 29)

| #   | Heuristic                       | Score | Key issue                                                                   |
| --- | ------------------------------- | ----- | --------------------------------------------------------------------------- |
| 1   | Visibility of System Status     | 3     | Плашка «сейчас» легла поверх идущей записи; шапка сетки режет первый час    |
| 2   | Match System / Real World       | 3     | `16/09/2026` в русском UI остался                                           |
| 3   | User Control and Freedom        | 3     | «Не пришёл» и «Все завершены» без отмены; возврат статуса есть, но в шторке |
| 4   | Consistency and Standards       | 2     | «Подтверждена» вернулась; «Ждут ответа 1» против 16; фильтр шире сетки      |
| 5   | Error Prevention                | 3     | Причина нуля появилась, но лежит ниже сгиба                                 |
| 6   | Recognition Rather Than Recall  | 3     | Шкала владелицы и полосы услуг без легенды                                  |
| 7   | Flexibility and Efficiency      | 3     | ⌘K, CSV, фильтры, роль-адаптивные вкладки; нет массового подтверждения      |
| 8   | Aesthetic and Minimalist Design | 3     | Спарклайн заполнил карточку дохода; розовая стена свободных окон осталась   |
| 9   | Error Recovery                  | 3     | «Нет новых окон» уже называет причину, но не рядом с кнопкой                |
| 10  | Help and Documentation          | 3     | Объяснение под каждым заголовком, сноска про бухучёт                        |

## Design Specificity

Специфичен. Шкала суток, чёрная карточка дохода как единственная инверсия, словарь салона («Сейчас в креслах», «Ждут отметки»), кольцо отсчёта на ресепшене. Из уровня выпадает «Рабочее время»: нативные `input[type=date]` с браузерным глифом.

Detector: 3 находки, все ложные (2 полосы-метки прототипа + цвет в тесте). Прогон без исключений — 180; настоящих пять: `.cal-filter__avatar` 10px при пороге 11, `.chip`/`.badge`/`.seg`/`.cb`/`.kbd` в kit.css со скруглениями 5–8px вместо пилюли, `.upload__thumb` 8px и `.world-gallery__card` 18px против таблицы §2.4, фокус-кольцо 6px на пилюлях, два устаревших комментария о радиусах (`tokens.css:275`, `app.css:2675`).
Browser: touch-цели на 390 чистые во всех шести экранах (76/74/118/92/52/60 контролов). Настоящих находок две: `.cal-card--team` с `padding:0` + `overflow:hidden` и липкой шапкой без отступа режет первую строку сетки; `cramped-padding` на `small.home-next__lead`.

## Regressions verified fixed

Значок роли ≥11px, `.btn` пилюлей через `--control-radius`, устаревшие утверждения о радиусах в §2.4 — все три держатся. Контраст тёмной темы: ноль пар ниже AA на четырёх экранах.

## Priority Issues

- [P0] «Рабочее время»: нативные даты, чипы дней 32px при пороге 44 (`app.css:607`, вне блока `pointer: coarse`), причина нуля в плитке предпросмотра ниже сгиба, пока отказ прибит в подвале. Fix: слот-паттерн для дат, чипы 44, причина строкой над кнопкой. /impeccable harden
- [P1] Плашка «Подтверждена» вернулась на главную: `showStatus={!hideStatus}` передаёт явный `true` и глушит правило «подтверждённое не подписываем» (`home-board.tsx:285`). Fix: не передавать проп там, где действует умолчание. /impeccable fix
- [P1] Слой «сейчас» закрывает то, ради чего существует: чёрная плашка времени лежит на идущей записи, «Выходной» набран на самой риске. Fix: плашку в колонку часов. /impeccable fix
- [P1] Фильтр шире сетки: `working` отдаёт всех активных, `teamColumns` выкидывает незанятого админа (`calendar-columns.ts:212`) — Ieva есть в чипах и в легенде шкалы, колонки у неё нет; шапка говорит «3 мастера работают» при пяти именах в легенде. Fix: строить фильтр и легенду из тех, у кого есть колонка. /impeccable fix
- [P1] Розовый назначен свободному времени вопреки закону 2 §2.0: `.cal-select` и `.calendar-agenda__chip` красят окна `--accent-ink`. Самое громкое пятно календаря — пустота. Fix: `--border-strong` / `--ink-faint`. /impeccable distill
- [P2] Шторка на телефоне 820px вместо обещанных `min(88dvh, 760px)`: `.amolie-app.sheet-panel` в неслоёном CSS перебивает Tailwind (`app.css:1784`). Плавающая карточка стала почти полным экраном.
- [P2] Десктоп 1440: правая колонка главной кончается на ~760 при левой до ~1430; под карточкой дохода в «Финансах» ~330px пустоты; справа от «Ждут отметки» пусто на всю высоту.
- [P2] Счётчики одним словом с разным охватом: «Ждут ответа 1» на календаре против «Нужен ответ 16» на главной; «СВОБОДНОЕ ВРЕМЯ 3» против «Свободных окон 4».
- [P2] Веса 500 и 600 по всему CSS кабинета при §2.2 «третьего веса в системе нет» — селектор-коэрция ловит только утилиты Tailwind, не собственные правила.
- [P2] Цветные полосы у услуг без ключа; «Любимые 0» и «Осторожно 0» кликабельны и ведут в пустоту; «Ещё» девятью строками без группировки.

## Persona Flags

- Alex (первый вход): упрётся в «Нет новых окон», причина — ниже сгиба.
- Sam (скорость): 16 заявок по одному касанию; единственная залитая розовая кнопка ведёт в «Новую запись».
- Casey (доступность): контраст и фокус в порядке; статус на сетке несут только цвет заливки и цвет точки — при дейтеранопии коралл и охра сольются, легенды нет.
- Соло-мастер одной рукой: на телефонном календаре четыре ряда управления до первой записи, сводка — под списком.
