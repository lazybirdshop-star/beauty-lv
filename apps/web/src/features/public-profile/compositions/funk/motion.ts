import type { MotionSpec } from '../../contracts/chrome';

/**
 * Хореография FUNK как данные (§10).
 *
 * Пружин у мира нет: перелёт несёт сама кривая `--ease-style`
 * (`cubic-bezier(0.2, 1.2, 0.4, 1)` — `pop` файла), и это осознанный
 * характер, а не небрежность. Мир нарисован как напечатанный объект,
 * который *ставят* на место; сглаженная кривая превратила бы щелчок в
 * вздох. Стоит ноль килобайт JS и работает на первом кадре SSR-выдачи.
 *
 * Сцены сменяются тем же щелчком, а не растворением: `wipe` — ближайшее
 * имя из контракта к тому, как этот мир меняет содержимое.
 */
export const motion: MotionSpec = {
  sheetInClass: 'sheet-panel-in',
  sheetOutClass: 'sheet-panel-out',
  stepTransition: 'wipe',
};
