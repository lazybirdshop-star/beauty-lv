# Features

Feature-Based Architecture on the frontend (see `ARCHITECTURE.md` §5): each business feature (booking, scheduling, organizations, ...) owns its own folder here with its components, hooks, and API calls. Route files under `src/app` stay thin and import from here. Empty pending Phase 1 (see `TASKS.md`).
