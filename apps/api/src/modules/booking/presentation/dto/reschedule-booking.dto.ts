import { IsUUID } from 'class-validator';

/**
 * Куда переносится визит.
 *
 * Только идентификатор окна, а не произвольное время: назвать час, которого
 * мастер не открывала, — её привилегия на собственном календаре, и публичная
 * страница такого права не имеет (то же правило, что у создания записи).
 */
export class RescheduleBookingDto {
  @IsUUID()
  publishedSlotId!: string;
}
