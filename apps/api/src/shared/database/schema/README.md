# Drizzle schema

Table definitions land here as flat `*.ts` files (one file per table or closely related group), matching the entities catalogued in `DATABASE.md`. Empty pending Phase 1 (see `TASKS.md`, epics A/O/S/SC/B).
